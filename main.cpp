#include <iostream>
#include <string>
#include "Big_Number.h"

using namespace std;

int main() {
   Big_Number a, b, c;
   int q;
   while(true) {
       cin >> a;
       cin >> b;
       a / b;
   // cout <<"c = " << (a - b) << endl;
   }
   return 0;
}